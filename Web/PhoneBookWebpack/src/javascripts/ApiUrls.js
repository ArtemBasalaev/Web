export default class ApiUrls {
    constructor(getContactsUrl, createContactUrl, deleteContactUrl, deleteContactsUrl) {
        this.getContactsUrl = getContactsUrl;

        this.createContactUrl = createContactUrl;

        this.deleteContactUrl = deleteContactUrl;

        this.deleteContactsUrl = deleteContactsUrl;
    }
};